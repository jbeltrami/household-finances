# Home Finances

## Agent skills

### Issue tracker

Issues live as markdown files under `.scratch/<feature>/issues/`. Specs live in
`docs/specs/`. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical roles, unchanged, written as a `Status:` line in each issue
file. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` and `docs/adr/` at the repo root. See
`docs/agents/domain.md`.
